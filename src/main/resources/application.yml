

spring:
  application:
    name: spring security
  security:
    user:
      name: user
      roles: user
  server:
    port: 8080
  datasource:
    url: jdbc:postgresql://localhost:5432/dmswjd
    username: dmswjd
    password:

  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
